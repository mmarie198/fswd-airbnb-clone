HTML-Only Airbnb Clone (Static) — README

A static HTML-only Airbnb-style landing page. No JavaScript, no backend, no build tools. Intended for layout practice and UI replication. Built with Bootstrap CSS.
